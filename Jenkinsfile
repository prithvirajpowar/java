pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'Java HelloWorld.java'
                sh 'Javac HelloWorld.java'
            }
        }
    }
}
